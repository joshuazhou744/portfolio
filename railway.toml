[build]
builder = "NIXPACKS"
rootDirectory = "backend"
buildCommand = "pip install -r requirements.txt"
engines = { python = "3.11" }

[deploy]
startCommand = "sh -c \"uvicorn app.main:app --host 0.0.0.0 --port ${PORT:-3001}\""
healthcheckPath = "/health"
healthcheckTimeout = 100
